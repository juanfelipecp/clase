materia
soy modificar
</form>

{{#each materia}}



<div class="container">
  <div class="row">
    <div class="col-sm">
      {{id_materia}}
    </div>
    <div class="col-sm">
      {{materia}}
    </div>
    <div>
      <a href="/materia/modificar/{{id_materia}}" type=" btn btn-green">Modificar</a>
    </div>
  </div>
</div>

{{/each}}

{{#each materiaid}}
<div class="container p-4">
  <div class="row">
    <div class="col-md-4">
      <div class="card">
        <div class="car-bady">
          <form action="/materia/modificaar/{{id_materia}}" method="post">
            <div class="form-group">
              <label for="">Registrar</label>
            </div>
            <div class="form-group">
              <input class="form-control" value="{{materia}}" name="materia" placeholder=" ingresar edad" id="">
              <div class="form-group">
                <button type="submit" value="{{id_materia}}" class="btn btn-success btn-block"> Enviar</button>
              </div>
          </form>
        </div>
      </div>
    </div>
  </div>


  {{/each}}